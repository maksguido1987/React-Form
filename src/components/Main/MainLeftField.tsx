import React, { FC } from 'react';
import Form from '../Form/Form';

const MainLeftField: FC = () => {
  return (
    <div className="left-main-container">
      <Form />
    </div>
  );
};

export default MainLeftField;
